<div class="container">
  <div class="row">
    <div class="col-md-6">
      <div class="position-relative">
        <% if @random_template.image.attached? %>
          <%= image_tag @random_template.image, class: 'img-fluid', style: 'max-height: 400px; width: 100%; object-fit: cover;' %>
          <div class="position-absolute top-50 start-50 translate-middle text-center text-white">
            <h1 class="text-black"><%= @quote.title %></h1>
          </div>
        <% end %>
      </div>
    </div>
    <!--
    div class="col-md-6">
      <h1><%= @quote.title %></h1>
      <p><strong>Legend:</strong> <%= @quote.legend.name %></p>
      <%= link_to 'Edit', edit_quote_path(@quote), class: 'btn btn-secondary' %>
      <%= link_to 'Back', quotes_path, class: 'btn btn-secondary' %>
    </div>
    -->
  </div>
</div>
